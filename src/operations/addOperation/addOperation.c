#include "addOperation.h"
#include <stdio.h>
#include <stdlib.h>

void **add(float **arrayA, float **arrayB) {
    int linesA = sizeof arrayA / sizeof arrayA[0];
    int columnsA = sizeof arrayA[0] / sizeof arrayA[0][0];

    int linesB = sizeof arrayB / sizeof arrayB[0];
    int columnsB = sizeof arrayB[0] / sizeof arrayB[0][0];


    return 0;
}